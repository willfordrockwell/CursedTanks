dist: trusty
sudo: required
language: cpp
os:
  - linux
install: sudo apt -y install libsfml-dev
compiler:
  - gcc
script:
  - make assem_lib
  - make
notifications:
  email:
    recipients:
      - willfordworks@gmail.com
    on_success: never
    on_failure: always